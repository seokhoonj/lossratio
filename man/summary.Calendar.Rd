% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/triangle.R
\name{summary.Calendar}
\alias{summary.Calendar}
\title{Summarise calendar-development statistics (Mean, Median, Weighted)}
\usage{
\method{summary}{Calendar}(object, ...)
}
\arguments{
\item{object}{An object of class \code{Calendar}.}

\item{...}{Unused; included for S3 compatibility.}
}
\value{
A \code{data.table} of class \code{"CalendarSummary"} with one row per
\verb{(group_var, calendar)} combination, containing:
\describe{
\item{n_obs}{Number of observations in the cell.}
\item{lr_mean}{Mean of loss ratios.}
\item{lr_median}{Median of loss ratios.}
\item{lr_wt}{Weighted loss ratio (\code{sum(loss) / sum(rp)}).}
\item{clr_mean}{Mean of cumulative loss ratios.}
\item{clr_median}{Median of cumulative loss ratios.}
\item{clr_wt}{Weighted cumulative loss ratio (\code{sum(closs) / sum(crp)}).}
}

The returned object preserves the attributes \code{group_var},
\code{calendar_var}, and \code{calendar_type}.
}
\description{
S3 method for \code{summary()} on \code{Calendar} objects. Computes
calendar-period summary statistics for loss ratios (\code{lr}) and
cumulative loss ratios (\code{clr}).

Where \code{\link[=summary.Triangle]{summary.Triangle()}} aggregates by \verb{(group_var, dev)} (cohort
× development), this method aggregates by \verb{(group_var, calendar)}
(calendar period) so the resulting table is indexed by calendar
diagonals rather than development periods.
}
\examples{
\dontrun{
cal <- build_calendar(df, group_var = cv_nm)
sm  <- summary(cal)
head(sm)
}

}
