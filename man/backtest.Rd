% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backtest.R
\name{backtest}
\alias{backtest}
\alias{print.backtest}
\alias{summary.backtest}
\alias{print.summary.backtest}
\title{Backtest a loss-ratio / chain ladder fit on existing data}
\usage{
backtest(x, holdout = 6L, fit_fn = fit_lr, value_var = "clr", ...)

\method{print}{backtest}(x, ...)

\method{summary}{backtest}(object, ...)

\method{print}{summary.backtest}(x, ...)
}
\arguments{
\item{x}{A \code{"triangle"} object (or a \code{"backtest"} object for the S3
\code{print()} method).}

\item{holdout}{Integer. Number of latest calendar diagonals to mask
before refitting. Default \code{6L}.}

\item{fit_fn}{Fitting function. Default \code{fit_lr} (stage-adaptive
loss-ratio projection); also supports \code{fit_cl} for single-column
chain ladder. If \code{fit_fn} does not have a \code{value_var} formal (as is
the case for \code{fit_lr}), \code{value_var} is used only to select the
comparison column on the fit's \verb{$full} table; arguments for the
fitter itself (e.g., \code{loss_var}, \code{exposure_var}, \code{method}) are
passed through \code{...}.}

\item{value_var}{Character scalar. Column to project and compare. For
\code{fit_lr} (default), must be one of \code{"closs"}, \code{"crp"}, or \code{"clr"}
(default), which map to \code{loss_proj}, \code{exposure_proj}, and \code{clr_proj}
respectively on \code{fit_lr$full}. For \code{fit_cl}, any column present in
\code{x}.}

\item{...}{Additional arguments passed to \code{fit_fn} (e.g., \code{method},
\code{alpha}, \code{recent}, \code{tail}).}

\item{object}{A \code{"backtest"} object. Used by the S3 \code{summary()} method.}
}
\value{
An object of class \code{"backtest"} with components:
\describe{
\item{\code{call}}{Matched call.}
\item{\code{data}}{Original \code{triangle}.}
\item{\code{masked}}{Triangle used for fitting (with held-out cells
removed).}
\item{\code{fit}}{The fit object returned by \code{fit_fn}.}
\item{\code{aeg}}{\code{data.table} of held-out cells with columns
\verb{(group_var, cohort, dev, value_actual, value_pred, aeg, calendar_idx)}.}
\item{\code{col_summary}}{Per-\code{dev} aggregate AEG (mean / median /
weighted / n).}
\item{\code{diag_summary}}{Per-calendar-diagonal aggregate AEG.}
\item{\code{value_var}, \code{holdout}, \code{fit_fn_name}}{Call metadata.}
\item{\code{group_var}, \code{cohort_var}, \code{dev_var}}{Variable name relays
from \code{x}.}
}
}
\description{
Hold out the latest \code{holdout} calendar diagonals from the input
\code{triangle}, refit the model on the earlier portion, project the
held-out cells, and compare the projection to the actual values
that were withheld.

The Actual-Expected Gap (AEG) is computed cell-wise as
\deqn{aeg = \frac{value_{proj}}{value_{actual}} - 1}
and aggregated by development period (\code{col_summary}) and by
calendar diagonal (\code{diag_summary}).
}
\examples{
\dontrun{
data(experience)
exp <- as_experience(experience)
tri <- build_triangle(exp, group_var = cv_nm)
bt <- backtest(tri, holdout = 6L)
print(bt)
summary(bt)
plot(bt)
}

}
\seealso{
\code{\link[=fit_lr]{fit_lr()}}, \code{\link[=fit_cl]{fit_cl()}}, \code{\link[=plot.backtest]{plot.backtest()}}
}
