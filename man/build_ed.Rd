% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ed.R
\name{build_ed}
\alias{build_ed}
\title{Build exposure-driven development data}
\usage{
build_ed(
  x,
  loss_var = "closs",
  exposure_var = "crp",
  min_exposure = 0,
  drop_invalid = FALSE
)
}
\arguments{
\item{x}{An object of class \code{"triangle"}.}

\item{loss_var}{A single cumulative loss variable. Default is
\code{"closs"}.}

\item{exposure_var}{A single cumulative exposure variable. Default is
\code{"crp"}.}

\item{min_exposure}{Minimum exposure required to compute \code{g}. If
\code{exposure_from <= min_exposure}, \code{g} is set to \code{NA}. Default is \code{0}.}

\item{drop_invalid}{Logical; if \code{TRUE}, rows with invalid (non-finite)
\code{g} values are dropped. Default is \code{FALSE}.}
}
\value{
A \code{data.table} with class \code{"ed"} containing:
\describe{
\item{\code{ata_from}}{Current development period.}
\item{\code{ata_to}}{Next development period.}
\item{\code{ata_link}}{Concatenated label \code{"ata_from-ata_to"}.}
\item{\code{loss_from}}{Cumulative loss \eqn{C^L_{i,k}}.}
\item{\code{loss_to}}{Cumulative loss \eqn{C^L_{i,k+1}}.}
\item{\code{delta_loss}}{Incremental loss \eqn{\Delta C^L_{i,k+1}}.}
\item{\code{exposure_from}}{Cumulative exposure \eqn{C^P_{i,k}}.}
\item{\code{exposure_to}}{Cumulative exposure \eqn{C^P_{i,k+1}}.}
\item{\code{g}}{Incremental loss intensity
\eqn{\Delta C^L_{i,k+1} / C^P_{i,k}}, or \code{NA} when
\code{exposure_from <= min_exposure}.}
}

The returned object carries the following attributes:
\code{group_var}, \code{cohort_var}, \code{dev_var}, \code{loss_var}, and
\code{exposure_var}.
}
\description{
Construct exposure-driven incremental development data from an object
of class \code{"triangle"}, typically produced by \code{\link[=build_triangle]{build_triangle()}}. This is the
foundational data structure for the exposure-driven (ED) model,
where incremental loss is modelled as a function of cumulative
exposure (risk premium) rather than cumulative loss.

The incremental loss intensity is defined as:

\deqn{g_{i,k} = \frac{\Delta C^L_{i,k+1}}{C^P_{i,k}}}

where \eqn{\Delta C^L_{i,k+1} = C^L_{i,k+1} - C^L_{i,k}} is the
incremental loss and \eqn{C^P_{i,k}} is the cumulative exposure
(risk premium) at development period \eqn{k}.
}
\examples{
\dontrun{
d <- build_triangle(df, group_var = cv_nm)
ed <- build_ed(d)
head(ed)
attr(ed, "loss_var")
attr(ed, "exposure_var")
}

}
\seealso{
\code{\link[=build_triangle]{build_triangle()}}, \code{\link[=summary.ed]{summary.ed()}}, \code{\link[=fit_ed]{fit_ed()}}
}
