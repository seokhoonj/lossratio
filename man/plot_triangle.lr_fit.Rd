% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lr-vis.R
\name{plot_triangle.lr_fit}
\alias{plot_triangle.lr_fit}
\title{Plot loss ratio projection as a triangle heatmap}
\usage{
\method{plot_triangle}{lr_fit}(
  x,
  what = c("full", "pred"),
  label_style = c("value", "detail"),
  label_args = list(),
  show_maturity = TRUE,
  digits = 0,
  amount_divisor = 1e+08,
  theme = c("view", "save", "shiny"),
  nrow = NULL,
  ncol = NULL,
  ...
)
}
\arguments{
\item{x}{An object of class \code{"lr_fit"}.}

\item{what}{One of \code{"full"} (observed + projected) or \code{"pred"}
(projected cells only). Default is \code{"full"}.}

\item{label_style}{One of \code{"value"} (clr only) or \code{"detail"}
(clr with loss/exposure amounts). Default is \code{"value"}.}

\item{label_args}{Named list of label appearance arguments.}

\item{show_maturity}{Logical; if \code{TRUE}, show maturity line.
Default is \code{TRUE}.}

\item{digits}{Number of decimal places for clr display.
Default is \code{0}.}

\item{amount_divisor}{Numeric divisor for amount display in
\code{"detail"} mode. Default is \code{1e8}.}

\item{theme}{Theme string.}

\item{nrow, ncol}{Facet dimensions.}

\item{...}{Additional arguments passed to \code{\link[=.switch_theme]{.switch_theme()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
Visualise an \code{"lr_fit"} object as a triangle-style heatmap of
cumulative loss ratios. Observed and projected cells are
distinguished by border style.
}
