% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/triangle-vis.R
\name{plot.triangle}
\alias{plot.triangle}
\title{Plot development trajectories with optional summary overlay}
\usage{
\method{plot}{triangle}(
  x,
  value_var = "clr",
  summary = FALSE,
  summary_min_n = 5L,
  amount_divisor = 1e+08,
  scales = c("fixed", "free_y", "free_x", "free"),
  theme = c("view", "save", "shiny"),
  ...
)
}
\arguments{
\item{x}{An object of class \code{triangle}.}

\item{value_var}{A single metric to plot. Must be one of:
\code{"lr"}, \code{"clr"},
\code{"loss"}, \code{"rp"}, \code{"margin"}, \code{"closs"}, \code{"crp"}, \code{"cmargin"},
\code{"loss_prop"}, \code{"rp_prop"}, \code{"closs_prop"}, or \code{"crp_prop"}.}

\item{summary}{Logical. If \code{FALSE} (default), shows raw cohort trajectories.
If \code{TRUE}, shows grey cohort trajectories with overlaid summary lines
(mean, median, weighted mean). Summary overlay is supported only for
\code{"lr"} and \code{"clr"}, and only when the x-axis variable is a development-period
variable (for example, \code{elap_m}, \code{elap_q}, \code{elap_h}, \code{elap_y}).}

\item{summary_min_n}{Optional minimum number of observations required for
the summary overlay to be considered reliable. When provided and
\code{summary = TRUE}, a vertical reference line is drawn at the midpoint just
before the first development period where \code{n_obs < summary_min_n} within each
facet. Default is \code{5}.}

\item{amount_divisor}{Numeric scaling factor used only for y-axis labels of
amount variables. Default is \code{1e8}.}

\item{scales}{Should scales be fixed (\code{"fixed"}), free (\code{"free"}),
or free in one dimension (\code{"free_x"}, \code{"free_y"})?}

\item{theme}{A string passed to \code{\link[=.switch_theme]{.switch_theme()}}
(\code{"view"}, \code{"save"}, \code{"shiny"}).}

\item{...}{Additional arguments passed to \code{\link[=.switch_theme]{.switch_theme()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
Visualise loss ratio or related metric trajectories across development time
from a \code{triangle} object.

The function supports two display modes:
\itemize{
\item \strong{Raw mode (\code{summary = FALSE})}: plots cohort-level trajectories
coloured by the period variable stored in the \code{triangle} object.
\item \strong{Summary mode (\code{summary = TRUE})}: plots all cohort trajectories
in grey and overlays three summary statistics:
\itemize{
\item Mean
\item Median
\item Weighted mean
}
}

Summary statistics are computed from \code{\link[=summary.triangle]{summary.triangle()}}.
}
\details{
The x-axis uses the development variable stored in \code{attr(x, "dev_var")}.
Cohort lines are grouped by the period variable stored in
\code{attr(x, "cohort_var")}, and facets are created from
\code{attr(x, "group_var")}.

The loss ratio is defined here as:
\deqn{lr = loss / rp}

where \code{rp} denotes risk premium rather than written premium.

The weighted mean is defined as:
\itemize{
\item \code{lr_wt = sum(loss) / sum(rp)}
\item \code{clr_wt = sum(closs) / sum(crp)}
}

Ratio and proportion metrics are plotted on the original scale and displayed
as percentages via y-axis labels. Amount metrics are plotted on the original
scale and displayed using y-axis labels scaled by \code{amount_divisor}.
}
