% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ata.R
\name{build_ata}
\alias{build_ata}
\title{Build age-to-age (ata) factors from \code{triangle} data}
\usage{
build_ata(
  x,
  value_var = "closs",
  weight_var = NULL,
  min_denom = 0,
  drop_invalid = FALSE
)
}
\arguments{
\item{x}{An object of class \code{triangle}.}

\item{value_var}{A single cumulative metric used to compute age-to-age.
Must be one of \code{"closs"}, \code{"crp"}, or \code{"clr"}.}

\item{weight_var}{An optional single cumulative metric used as weights
in WLS estimation via \code{\link[=summary.ata]{summary.ata()}}. Must be one of \code{"closs"},
\code{"crp"}, or \code{"clr"}, and must differ from \code{value_var}. Typical use
is \code{weight_var = "crp"} when \code{value_var = "clr"}, since \code{clr} values
are ratios and carry no exposure information. When \code{NULL} (default),
\code{value_from} is used as the WLS weight, which corresponds to the
standard volume-weighted chain ladder.}

\item{min_denom}{Minimum denominator required to compute age-to-age.
If \code{value_from <= min_denom}, \code{ata} is set to \code{NA}. This is useful
when early development periods contain structural zeros
(for example, waiting periods in life insurance).}

\item{drop_invalid}{Logical; if \code{TRUE}, rows with invalid (non-finite)
age-to-age factors are dropped. Useful for clean output when passing
to \code{\link[=summary.ata]{summary.ata()}} or \code{\link[=find_ata_maturity]{find_ata_maturity()}}. When \code{FALSE} (default),
all rows are retained, preserving the full triangle structure for
diagnostic visualisation via \code{\link[=plot_triangle.ata]{plot_triangle.ata()}}.}
}
\value{
A data.frame with class \code{"ata"} containing:
\describe{
\item{\code{ata_from}}{Current development period.}
\item{\code{ata_to}}{Next development period.}
\item{\code{value_from}}{Current cumulative value.}
\item{\code{value_to}}{Next cumulative value.}
\item{\code{ata}}{Age-to-age factor (\code{value_to / value_from}), or \code{NA}
when \code{value_from <= min_denom}.}
\item{\code{weight}}{Weight values at \code{ata_from} derived from
\code{weight_var}. Only present when \code{weight_var} is supplied.}
}

The returned object carries the following attributes:
\code{group_var}, \code{cohort_var}, \code{dev_var}, \code{value_var}, and
\code{weight_var}.
}
\description{
Construct age-to-age development factors from an object of class \code{triangle},
typically produced by \code{\link[=build_triangle]{build_triangle()}}. Age-to-age factors are calculated
from cumulative values such as \code{closs} or \code{crp}.

Age-to-age is defined as:
\deqn{ata_{k \to k+1} = value_{k+1} / value_k}

where \code{value_k} is the selected cumulative metric at development period \code{k}.
}
\examples{
\dontrun{
d <- build_triangle(df, group_var = cv_nm)

ata1 <- build_ata(d, "closs")
ata2 <- build_ata(d, "crp")
ata3 <- build_ata(d, "clr", weight_var = "crp")

head(ata1)
attr(ata1, "value_var")
attr(ata1, "weight_var")
}

}
\seealso{
\code{\link[=build_triangle]{build_triangle()}}, \code{\link[=summary.ata]{summary.ata()}}, \code{\link[=fit_ata]{fit_ata()}}
}
