% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{get_recent_weights}
\alias{get_recent_weights}
\title{Recent-diagonal weights for a development triangle}
\usage{
get_recent_weights(weights, recent)
}
\arguments{
\item{weights}{A triangle-shaped numeric matrix, with origin periods
as rows and development periods as columns. Unobserved future cells
should be \code{NA}.}

\item{recent}{Optional positive integer: the number of most recent
calendar diagonals to keep. When missing or \code{NULL}, \code{weights} is
returned unchanged.}
}
\value{
A numeric matrix of the same shape as \code{weights}.
}
\description{
Returns a weight matrix that restricts a development triangle to its
most recent \code{recent} calendar diagonals. Cells on or after the last
\code{recent} diagonals retain their input values; earlier observed cells
are set to \code{0}. Cells that are \code{NA} in the input (not yet observed)
are left as \code{NA}.

This is a standard construct for restricting chain-ladder estimation
to recent calendar periods when older experience is considered less
representative of current conditions (e.g. after a rate change or a
claim-handling reform).
}
\section{Handling of NA cells}{

\code{NA} cells in the input (not yet observed) remain \code{NA} in the output.
They are semantically distinct from the \code{0} cells produced by the
recency filter, which represent observed values explicitly excluded
from the current weighting scheme. Callers who want both to behave
identically can post-process with \code{w[is.na(w)] <- 0}.
}

\examples{
\dontrun{
m <- ChainLadder::RAA
get_recent_weights(m)       # unchanged (no `recent` supplied)
get_recent_weights(m, 3)    # keep only the last 3 calendar diagonals
}

}
