% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/triangle.R
\name{build_total}
\alias{build_total}
\title{Build a total development summary from experience data}
\usage{
build_total(
  df,
  group_var,
  cohort_var = "uym",
  dev_var = "elap_m",
  value_var = c("loss", "rp"),
  period_from = NULL,
  period_to = NULL,
  fill_gaps = FALSE
)
}
\arguments{
\item{df}{A data.frame containing experience data.}

\item{group_var}{Grouping variable(s).}

\item{cohort_var}{A single period variable. This may be an underwriting
period (\code{uym}, \code{uyq}, \code{uyh}, \code{uy}) or a calendar period
(\code{cym}, \code{cyq}, \code{cyh}, \code{cy}). Default \code{"uym"}.}

\item{dev_var}{A single development variable used to count observed periods.
Default \code{"elap_m"}.}

\item{value_var}{Value variables to aggregate. Must include both \code{"loss"}
and \code{"rp"}. Default \code{c("loss", "rp")}.}

\item{period_from}{Optional lower bound for \code{cohort_var}. Only rows with
\code{cohort_var >= period_from} are kept. May be supplied as \code{Date},
character, or any value coercible to \code{Date}. Default \code{NULL}.}

\item{period_to}{Optional upper bound for \code{cohort_var}. Only rows with
\code{cohort_var <= period_to} are kept. May be supplied as \code{Date},
character, or any value coercible to \code{Date}. Default \code{NULL}.}

\item{fill_gaps}{Logical; if \code{TRUE}, zero-fill missing
\verb{(group_var, cohort_var, dev_var)} cells before aggregation so
that every cohort has a consecutive \code{dev_var} sequence. Default
\code{FALSE}. Note that filling inflates \code{n_obs} (counts filled rows as
observed periods); use \code{\link[=validate_triangle]{validate_triangle()}} to inspect first.}
}
\value{
A data.frame with class \code{"total"} containing:
\describe{
\item{n_obs}{Number of observed development periods}
\item{sales_start}{First observed period}
\item{sales_end}{Last observed period}
\item{loss}{Total loss}
\item{rp}{Total risk premium}
\item{lr}{Total loss ratio (\code{loss / rp})}
\item{loss_prop}{Share of total loss}
\item{rp_prop}{Share of total risk premium}
}
}
\description{
Aggregate \code{loss} and \code{rp} by group and compute the corresponding total
loss ratio over a selected period window.

This function is intended for high-level portfolio comparison across
groups such as products, coverages, or channels. It summarises:
\itemize{
\item the number of observed periods (\code{n_obs})
\item the first and last observed periods (\code{sales_start}, \code{sales_end})
\item total \code{loss} and total \code{rp}
\item total loss ratio (\code{lr = loss / rp})
\item each group's share of total loss and risk premium
}

If \code{period_from} and/or \code{period_to} are supplied, the input data are first
restricted to that period window before aggregation. This is useful when
comparing groups on a common period basis.
}
\examples{
\dontrun{
build_total(df, cv_nm)

build_total(
  df,
  cv_nm,
  period_from = "2023-01-01",
  period_to   = "2023-12-01"
)
}

}
