% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-instead.R
\name{.capture_names}
\alias{.capture_names}
\title{Capture and normalize column specifications into names}
\usage{
.capture_names(data, cols)
}
\arguments{
\item{data}{A data.frame (or tibble/data.table).}

\item{cols}{A column specification (see description).}
}
\value{
Character vector of column names; \code{character(0)} if absent.
}
\description{
Resolves a flexible column specification (character vector, numeric
indices, a variable, or a bare symbol / \code{c(a, b)} / \code{.(a, b)} call) into
a character vector of column names guaranteed to exist in \code{data}.

Use inside another function by capturing and unquoting with
\code{!!rlang::enquo(x)}:

\if{html}{\out{<div class="sourceCode">}}\preformatted{f <- function(data, cols) \{
  .capture_names(data, !!rlang::enquo(cols))
\}
}\if{html}{\out{</div>}}
}
\keyword{internal}
