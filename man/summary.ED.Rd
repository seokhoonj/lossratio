% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ed.R
\name{summary.ED}
\alias{summary.ED}
\title{Summarise ED intensity statistics}
\usage{
\method{summary}{ED}(object, alpha = 1, digits = 5, ...)
}
\arguments{
\item{object}{An object of class \code{"ED"}, typically produced by
\code{\link[=build_ed]{build_ed()}}.}

\item{alpha}{Numeric scalar controlling the variance structure in the
WLS fit. Default is \code{1}.}

\item{digits}{Number of decimal places to round numeric columns.
Default is \code{5}. Pass \code{NULL} to skip rounding.}

\item{...}{Additional arguments passed to the internal WLS estimation.}
}
\value{
A \code{data.table} with class \code{"EDSummary"} containing one row
per development link with descriptive statistics and WLS estimates.
}
\description{
Compute group-wise summary statistics for incremental loss intensity
\eqn{g} from an object of class \code{"ED"}. This function serves two
purposes:

\enumerate{
\item \strong{Diagnostics}: provides descriptive statistics
(\code{mean}, \code{median}, \code{wt}, \code{cv}) that help the user assess the
stability and consistency of observed \eqn{g} values across
cohorts.
\item \strong{Estimation}: fits a no-intercept weighted least
squares model per development link to produce the WLS-estimated
intensity (\code{g}), its standard error (\code{g_se}), relative standard
error (\code{rse}), and residual sigma (\code{sigma}). These are used
downstream by \code{\link[=fit_ed]{fit_ed()}}.
}
}
\section{Relationship between \code{wt} and \code{g}}{

Both \code{wt} and \code{g} are weighted averages of the observed intensities,
but they differ in how weights are assigned:

\describe{
\item{\code{wt}}{Exposure-weighted mean:
\eqn{wt = \sum \Delta C^L_{i,k+1} / \sum C^P_{i,k}}.
Computed from all rows where both values are finite.
Independent of \code{alpha}.}
\item{\code{g}}{WLS-estimated intensity from
\code{lm(delta_loss ~ exposure_from + 0)}. Only rows where
\code{exposure_from > 0} are used. When \code{alpha = 2}, \code{g} and \code{wt}
are numerically equivalent.}
}
}

\seealso{
\code{\link[=build_ed]{build_ed()}}, \code{\link[=fit_ed]{fit_ed()}}
}
