# Format numbers with comma as thousands separator

Format numbers with comma as thousands separator

## Usage

``` r
.as_comma(x, digits = 0L)
```

## Arguments

- x:

  Integer or numeric vector.

- digits:

  Number of digits after the decimal point. Default `0`.

## Value

Character vector of the same length as `x`.
