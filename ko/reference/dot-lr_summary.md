# Summarise an `LRFit` object by cohort

Summarise an `LRFit` object by cohort

## Usage

``` r
.lr_summary(x)
```

## Arguments

- x:

  An object of class `"LRFit"`.

## Value

The input object with `$summary` set.
