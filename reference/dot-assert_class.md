# Assert that `x` inherits from the given class(es)

Assert that `x` inherits from the given class(es)

## Usage

``` r
.assert_class(x, classes)
```

## Arguments

- x:

  An R object.

- classes:

  Character vector of expected class names.

## Value

Invisibly returns `x` on success; otherwise aborts.
