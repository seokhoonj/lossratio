# Summarise an `lr_fit` object by cohort

Summarise an `lr_fit` object by cohort

## Usage

``` r
.lr_summary(x)
```

## Arguments

- x:

  An object of class `"lr_fit"`.

## Value

The input object with `$summary` set.
