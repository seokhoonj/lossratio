# Assert that `x` has nonzero length

Assert that `x` has nonzero length

## Usage

``` r
.assert_length(x)
```

## Arguments

- x:

  An R object.

## Value

Invisibly returns `x` on success; otherwise aborts.
