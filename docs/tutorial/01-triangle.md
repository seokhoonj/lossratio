# 1장. 손해율과 코호트 삼각형

```{admonition} 이 장에서 배우는 것
:class: tip

- 손해율이 무엇이고, 장기 건강보험에서 왜 측정하기 어려운가
- 분모효과와 관성 — 시간이 경과하는 것만으로(실제 위험 변화 없이) 누적 손해율이 흘러가는 현상
- 경험 데이터의 구조와 코호트라는 개념
- 코호트 x 경과 삼각형을 만드는 `Triangle`
- 누적값과 증분값, 그리고 삼각형을 들여다보는 법
```

이 장은 전체 흐름의 **첫 단계**, 경험 데이터를 삼각형으로 정리하는
자리입니다.

```{mermaid}
flowchart LR
  A["경험 데이터"] --> B["Triangle<br/>삼각형 (이 장)"]
  B --> C["link 진단"]
  C --> D["예측"]
  D --> E["불확실성"]
  E --> F["regime · 백테스트"]
  style B fill:#ffe08a,stroke:#d39e00,stroke-width:2px
```

## 1.1 손해율이란

**손해율(loss ratio)**은 받은 보험료 대비 나간 손해의 비율입니다.

$$
\text{(위험) 손해율} = \frac{\text{손해액}}{\text{(위험) 보험료}}
$$

손해율이 1보다 작으면 보험료가 손해를 덮고도 남았다는 뜻이고, 1을
넘으면 손해가 보험료를 초과했다는 뜻입니다. 보험회사가 상품의
수익성을 가늠하는 가장 기본적인 지표입니다.

```{admonition} 어떤 손해율인가
:class: note

손해율은 사실 하나의 정의가 아닙니다. 분자에 들어가는 손해부터가
여러 가지입니다. 실제로 지급한 보험금만 측정할 수도 있고(지급손해액),
아직 지급은 안 했어도 이번 기간에 발생한 것으로 인식한 몫까지 더할
수도 있습니다(발생손해액). 여기에 손해조사비를 포함하느냐, 분모를
영업보험료로 보느냐 경과보험료나 위험보험료로 보느냐가 더해지면
손해율은 값도 의미도 제각각이 됩니다. 어느 하나가 옳고 그른 것이
아니라, 보려는 목적이 다른 것입니다.

:::{admonition} 지급손해액과 발생손해액
:class: hint

- **지급손해액** -- 그 기간에 실제로 지급한 보험금. 지갑에서 나간 돈.
- **지급준비금** -- 이미 사고는 났지만 아직 지급하지 않은 보험금에
  대비해 쌓아 두는 부채(미지급 청구분 + 아직 보고되지 않은 IBNR).
- **발생손해액** = 지급보험금 + 지급준비금 변동(기말 - 기초). 그 기간에
  실제로 *발생한* 손해 전체로, 앞으로 나갈 것까지 포함합니다. 사고와
  지급 사이의 시차가 큰 장기 보험에서는 지급손해액보다 발생손해액이
  손해의 실상에 가깝습니다.
:::

이 문서에서 손해율이라 하면 **경과기간별 위험보험료 기준 발생손해율**을
뜻합니다. 위 수식에 (위험)을 붙인 것은 이 때문입니다. 분모는 위험을
덮기로 책정한 위험보험료(부가비용과 저축 부분을 뺀 순보험료)이고, 분자는
그에 정합하는 발생손해액(손해조사비와 구상은 제외)입니다.

회사 전체의 수익성을 보는 일반 손해율과 달리, 이 손해율은 예정위험률
(가격 가정)이 실제 손해를 감당할 수준이었는지를 봅니다. 장기 건강보험에서
핵심 질문이 바로 이것 — 지금 받는 위험보험료가 앞으로의 손해를 감당할 수
있는가 — 이기 때문입니다.
```

문제는 이 값을 **언제** 측정하느냐입니다. 제조업이라면 물건을 판 그
자리에서 매출과 원가가 거의 확정됩니다. 그러나 보험은 보험료를 먼저
받고 보험금은 나중에, 그것도 한참에 걸쳐 나갑니다. 계약을 막
인수했을 때 측정한 손해율과 몇 년이 지나 측정한 손해율은 전혀 다른
값입니다.

## 1.2 분모효과와 관성

장기 건강보험에서 손해율을 다룰 때 가장 먼저 마주치는 함정은,
**시간이 경과하는 것만으로도 — 실제 위험이 나빠지지 않았는데도 — 누적
손해율 숫자가 변한다**는 점입니다. 한 코호트의 누적 손해율이 시간이
경과함에 따라 어떻게 움직이는지 직접 보겠습니다 (수치는 뒤에서 데이터를
불러와 확인합니다).

| 경과월 | 누적 손해 | 누적 보험료 | 누적 손해율 |
|---|---|---|---|
| 1 | 0.11억 | 0.31억 | 0.34 |
| 2 | 0.61억 | 0.63억 | 0.96 |
| 3 | 1.14억 | 0.93억 | 1.21 |
| 5 | 1.98억 | 1.55억 | 1.28 |
| 6 | 2.51억 | 1.86억 | 1.35 |

같은 코호트인데도 누적 손해율이 0.34에서 1.35로 계속 올라갑니다. 이는
실제로 위험이 나빠져서가 아니라, **분자(손해)와 분모(보험료)가 서로
다른 속도로 쌓이기 때문**입니다. 장기 건강보험은 보장 초기에 손해가
몰리는 경우가 많아, 초기에는 손해율이 빠르게 오르다 차차 완만해집니다.

```{admonition} 분모효과와 관성
:class: important

**분모효과(denominator effect)** — 누적 손해율의 분모인 누적
보험료가 시간이 경과하면서 계속 불어나, 분자의 변화를 희석해 신호를
무디게 만드는 현상.

**관성(inertia)** — 그렇게 누적값이 커질수록 최근 기간에 일어난
변화가 전체 비율에 거의 영향을 주지 못하게 되는 둔감함.
```

그래서 손해율 분석의 출발점은 "지금 측정한 손해율"을 그대로 믿는 것이
아니라, **손해가 시간이 경과함에 따라 어떻게 움직이는지**를 먼저
파악하는 것입니다. lossratio는 이렇게 시간에 따라 움직이는 손해율을
코호트 삼각형으로 정리하는 데서 시작합니다.

## 1.3 경험 데이터의 구조

분석의 원재료는 **경험 데이터(experience data)**입니다. 한 행이
"어떤 코호트가, 경과 몇 개월 시점에, 그 기간 동안 손해와 보험료가
얼마였는가"를 담은 long-format 표입니다.

lossratio에는 시연용 합성 경험 데이터가 들어 있습니다.

```python
import polars as pl
import lossratio as lr

df = lr.load_experience()
df.select(["coverage", "uy_m", "cy_m", "duration_m", "incr_loss", "incr_premium"]).head(3)
#> shape: (3, 6)
#> ┌──────────┬────────────┬────────────┬────────────┬───────────┬──────────────┐
#> │ coverage ┆ uy_m       ┆ cy_m       ┆ duration_m ┆ incr_loss ┆ incr_premium │
#> │ ---      ┆ ---        ┆ ---        ┆ ---        ┆ ---       ┆ ---          │
#> │ str      ┆ date       ┆ date       ┆ i64        ┆ i64       ┆ i64          │
#> ╞══════════╪════════════╪════════════╪════════════╪═══════════╪══════════════╡
#> │ CI       ┆ 2023-01-01 ┆ 2023-01-01 ┆ 1          ┆ 1418956   ┆ 1356200      │
#> │ CI       ┆ 2023-01-01 ┆ 2023-02-01 ┆ 2          ┆ 73602     ┆ 1274360      │
#> │ CI       ┆ 2023-01-01 ┆ 2023-03-01 ┆ 3          ┆ 420092    ┆ 1384735      │
#> └──────────┴────────────┴────────────┴────────────┴───────────┴──────────────┘
```

핵심 열은 다음과 같습니다.

```{list-table}
:header-rows: 1
:widths: 22 78

* - 열
  - 의미
* - `coverage`
  - 담보 구분 (CI / CAN / HOS / SUR) — 그룹 변수
* - `uy_m`
  - 인수 연월 (underwriting year-month) — **코호트**를 정의
* - `cy_m`
  - 달력 연월 (calendar year-month) — 실제로 손해가 난 시점
* - `duration_m`
  - 경과월 (duration (months)) — 인수 후 몇 번째 달인가
* - `incr_loss`
  - 그 기간 동안의 손해액 (증분, incremental loss)
* - `incr_premium`
  - 그 기간 동안의 위험보험료 (증분, incremental premium)
```

여기서 **코호트(cohort)**는 같은 시점에 인수된 계약들의 묶음입니다.
2023년 1월에 인수된 계약들이 하나의 코호트, 2월에 인수된 계약들이
또 다른 코호트입니다. 코호트를 고정해 두고 경과월(`duration_m`)을 따라가면,
그 계약 집단의 손해가 시간이 지나면서 어떻게 움직이는지를 볼 수 있습니다.

이 데이터에는 월(M) 외에 분기(Q)·반기(H)·연(Y) 단위의 기간 열도 함께
들어 있어, 더 포괄적인 집계 주기로도 분석할 수 있습니다. 직접 가진
데이터라면 월 단위 표만 있어도 `derive_grain_columns()`로 나머지
주기 열을 만들 수 있습니다.

## 1.4 코호트 x 경과 삼각형

`Triangle`은 이 long-format 경험 데이터를 받아 **코호트 x 경과 기간**
삼각형으로 집계합니다. 생성자가 스키마를 검증하고, 누적값과 파생
열을 자동으로 더합니다.

이번 튜토리얼은 구조 변화가 한 번 심어져 있는 수술담보에 집중하겠습니다.

```python
df_sur = df.filter(pl.col("coverage") == "SUR")
tri = lr.Triangle(df_sur, groups="coverage")
tri
#> <Triangle: 666 rows, 1 groups, 36 cohorts x 36 durations (M)>
```

`groups` 인자에는 그룹을 나누는 열을 넘깁니다. 여러 담보를 한꺼번에
넣으면 이후 모든 추정·탐지가 그룹별로 따로 수행됩니다.

```{admonition} 왜 월 단위가 기본인가 — 집계 주기의 목적
:class: note

이 패키지는 **월(M) 단위를 기본**으로 둡니다. lossratio의 핵심 가치가
*손해율 수준(level)을 가장 이르게, 그러나 믿을 수 있게 읽어내는 속도*에
있기 때문입니다 — 분기·반기로 묶으면 그만큼 첫 신호가 늦어집니다. 월
단위의 출렁임은 2장에서 볼 ATA 인자의 변동(CV·RSE) 진단으로 다스리므로,
*집계로 노이즈를 죽이는* 대신 *판단 기준으로 노이즈를 다스립니다*.
분기·반기·연 단위는 신호를 이르게 읽을 필요가 없는 **구조 분석**
(수렴·대각선 효과·regime 탐지)에서, 포트폴리오 크기에 맞춰 신뢰도를
키울 때 씁니다.

이 튜토리얼의 삼각형 그림은 칸을 읽기 쉽게 **분기(Q) 단위**로 집계해
보여 줍니다. 월 단위는 경과 칸이 30개를 넘어 한 장에 너무 빽빽하기
때문입니다. 분석 자체는 월 단위로 하되, *그림으로 구조를 눈에 익힐
때는* 분기로 묶어 봅니다.
```

## 1.5 누적과 증분

삼각형의 각 칸은 한 코호트가 특정 경과월에 도달했을 때의 값입니다.
lossratio는 **누적값을 기본**으로 삼고, 기간별 증분값에는 `incr_`
접두사를 붙입니다.

```python
tri.df.select(["cohort", "duration", "incr_loss", "loss", "ratio", "incr_ratio"]).head(4)
#> shape: (4, 6)
#> ┌────────────┬─────┬─────────────┬──────────────┬──────────┬────────────┐
#> │ cohort     ┆ duration ┆ incr_loss   ┆ loss         ┆ ratio    ┆ incr_ratio │
#> │ ---        ┆ --- ┆ ---         ┆ ---          ┆ ---      ┆ ---        │
#> │ date       ┆ i64 ┆ f64         ┆ f64          ┆ f64      ┆ f64        │
#> ╞════════════╪═════╪═════════════╪══════════════╪══════════╪════════════╡
#> │ 2023-01-01 ┆ 1   ┆ 1.0548252e7 ┆ 1.0548252e7  ┆ 0.336916 ┆ 0.336916   │
#> │ 2023-01-01 ┆ 2   ┆ 5.0194064e7 ┆ 6.0742316e7  ┆ 0.961682 ┆ 1.575738   │
#> │ 2023-01-01 ┆ 3   ┆ 5.2758793e7 ┆ 1.13501109e8 ┆ 1.214421 ┆ 1.741297   │
#> │ 2023-01-01 ┆ 4   ┆ 3.8939539e7 ┆ 1.52440648e8 ┆ 1.224456 ┆ 1.254679   │
#> └────────────┴─────┴─────────────┴──────────────┴──────────┴────────────┘
```

- `incr_loss` — 그 경과월 한 기간 동안의 손해 (증분)
- `loss` — 경과 1부터 그 시점까지 합산한 손해 (누적)
- `ratio` — 누적 손해 / 누적 보험료 (누적 손해율)
- `incr_ratio` — 그 기간의 증분 손해 / 증분 보험료 (기간별 손해율)

1.2절에서 본 분모효과는 바로 이 `ratio` 열에서 드러납니다. 누적
`ratio`는 경과에 따라 완만하게 커지지만, `incr_ratio`는 그 기간만의
순수한 손해율이라 분모효과의 영향을 받지 않습니다.

두 관점은 **맞교환** 관계입니다. 누적 `ratio`는 그 시점까지의 모든
관측을 합쳐 **신뢰도(credibility)가 높지만**, 분모효과·관성에 눌려 최근
변화에는 둔합니다. 반대로 `incr_ratio`는 그 기간의 신호에 **민첩하게
반응하지만**, 월 한 칸의 얇은 표본이라 그만큼 출렁입니다(분산이 큼).
어느 한쪽이 옳은 게 아니라 *신뢰도와 반응성의 맞교환*이며, 두 관점을
나란히 보는 것이 손해율 분석의 기본기입니다.

## 1.6 삼각형은 어떻게 만들어지나

원자료의 한 행은 "어느 코호트(`uy_m`)가 어느 달력 시점(`cy_m`)에" 손해를
냈는지를 담고 있습니다. 이 두 축 그대로 손해율을 칸 색으로 칠하면(아래 첫
그림), 코호트마다 출발점이 어긋난 **계단 모양**이 나옵니다. 각 코호트는 자기
인수 시점에서 시작해 달력을 따라 오른쪽 아래로 흘러갑니다.

같은 데이터를 **경과기간**(`duration = cy_m - uy_m`, 인수 후 몇 번째 달인가)에
맞춰 다시 정렬하면(아래 둘째 그림), 모든 코호트의 출발점이 경과 1로
끌려오면서 계단이 왼쪽으로 모여 **직각삼각형**이 됩니다. 이렇게 정렬해야
서로 다른 코호트를 같은 경과 시점에서 세로로 비교할 수 있습니다.

도식으로 보면 — 위는 달력(`cy`) 축의 계단 배치, 아래는 경과(`duration = cy - uy`)로
당겨 모든 코호트의 출발을 경과 1 에 맞춘 직각삼각형이다 (`X` = 관측 셀,
`.` = 미관측 = 예측 대상):

```text
  calendar layout (staggered)
   cy ->  1   2   3   4
 uy +----------------+
  1 | X   X   X   X  |
  2 |     X   X   X  |
  3 |         X   X  |
  4 |             X  |
    +----------------+
        |   realign by  duration = cy - uy
        v
  duration-aligned (right triangle)
   duration -> 1   2   3   4
 uy +----------------+
  1 | X   X   X   X  |
  2 | X   X   X   .  |
  3 | X   X   .   .  |
  4 | X   .   .   .  |
    +----------------+
```

```{eval-rst}
.. plot::
   :context:
   :nofigs:
   :include-source: false

   import polars as pl
   import lossratio as lr

   df = lr.load_experience()
   df_sur = df.filter(pl.col("coverage") == "SUR")
   tri = lr.Triangle(df_sur, groups="coverage", grain="Q")

.. plot::
   :context: close-figs
   :caption: 달력 위치 그대로 그린 분기별 손해율. 코호트마다 출발점이 어긋난 계단 모양이다.

   tri.plot_triangle(metric="ratio", x_axis="calendar")

.. plot::
   :context: close-figs
   :caption: 경과기간에 맞춰 정렬한 분기별 손해율. 출발점이 경과 1로 모여 직각삼각형이 된다.

   tri.plot_triangle(metric="ratio", x_axis="duration")
```

오른쪽 아래가 비어 있는 것은 최근에 인수된 코호트일수록 아직 경과 기간이 짧아
관측된 칸이 적기 때문입니다. 이 **비어 있는 오른쪽 아래를 채우는 일** — 미관측
구간의 손해율을 예측하는 것이 앞으로 이 튜토리얼이 다룰 과제입니다.

## 1.7 함께 보기

- {doc}`2장 — 손해 진전 속도 <02-ata>`: 이웃한 경과를 이어
  손해 진전 속도(ATA 인자)를 손으로 구하고, 경과별로 읽으며, ATA 인자가
  코호트 간에 안정되는 구간을 진단합니다.
- {doc}`API 레퍼런스 <../api>`의 `Triangle`, `load_experience`,
  `derive_grain_columns`
